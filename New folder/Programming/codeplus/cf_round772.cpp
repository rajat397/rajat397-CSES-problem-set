#include<bits/stdc++.h>

using namespace std;
void solve(long long int a[],long long int n,long long cnt)
{
   

}

int main()

{
 long long int n,p,i;
    cin>>n>>p;
    long long int a[n],cnt=n;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    sort(a,a+n);

solve(a,n,cnt);
}